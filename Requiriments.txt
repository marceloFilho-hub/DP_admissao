# OCR avançado para extrair texto de imagens e PDFs escaneados
paddleocr

# Processamento de imagens: limpeza, binarização, remoção de ruído, deskew
opencv-python

# Manipulação simples de imagens (abrir, salvar, converter formatos)
pillow

# Leitura e conversão de PDF para imagens (PyMuPDF)
pymupdf

# Carregamento de variáveis de ambiente (.env)
python-dotenv

# Leitura de e-mails via IMAP e download de anexos
imap-tools

# Framework moderno para criar APIs (processamento, integração com RH etc.)
fastapi

# Servidor ASGI para rodar a API FastAPI
uvicorn

